package com.google.gdata.youtube

/**
 * You tube schema constants.
 * 
 * @author Iulian Dragos
 */
object Schemas {
  /** User favorites video feed. */
  final val USER_FAVORITES = "http://gdata.youtube.com/schemas/2007#user.favorites"
  
  /** User contacts feed. */
  final val USER_CONTACTS = "http://gdata.youtube.com/schemas/2007#user.contacts"
  
  /** User inbox feed. */
  final val USER_INBOX = "http://gdata.youtube.com/schemas/2007#user.inbox"
  
  /** User playlists feed. */
  final val USER_PLAYLISTS = "http://gdata.youtube.com/schemas/2007#user.playlists"
  
  /** User subscriptions feed. */
  final val USER_SUBSCRIPTIONS = "http://gdata.youtube.com/schemas/2007#user.subscriptions"
  
  /** User uploads feed. */
  final val USER_UPLOADS = "http://gdata.youtube.com/schemas/2007#user.uploads"
}
